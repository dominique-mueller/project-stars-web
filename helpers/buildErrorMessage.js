var logger = require('../adapters/logger.js');

module.exports = {
	function validationError(error, model){
		var filterInputAndField,
			filterRegEx = /(`.+?`)(`.+?`)/, // .+? is a non-greey expression for anything between the accent aigu (`) 
			
	}
}